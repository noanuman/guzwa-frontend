# ARCHITECTURE — Master Index

Each sub-directory documents one module of the GUZWA codebase.

## Modules

| Module | Path | Description |
|--------|------|-------------|
| *(none yet — add as modules are built)* | | |

> When creating a new module, add a sub-dir here with its own INDEX.md + detail MDs. Then add a row above and update ROUTER.md.
